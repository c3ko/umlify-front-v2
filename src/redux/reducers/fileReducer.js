export default (state = {}, action) => {
    switch(action.type){
        case 'ADD_NEW_FILE':
            // Add current contents of 
            return {

            }
    
    default:
        return state
    }
}